# src/text — テキスト

担当: **text-budget**（`.claude/agents/text-budget.md`）

辞書展開、台詞テンプレート、ウィンドウ描画。

辞書圧縮は**初期から必須**。台詞が増えてから対応するのでは遅い
（企画書 §7: 後回しにすると最後に削る対象は必ずキャラクターの個性になる）。

実装開始は **P7**、ただし辞書圧縮の枠組みは早期に置く。
