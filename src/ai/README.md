# src/ai — 自律仲間AI

担当: **ai-dev**（`.claude/agents/ai-dev.md`）

6型（突撃 / 牽制 / 回収 / 庇護 / 遠隔 / 万能）の行動ロジック。

**型の個性はパラメータ表で表現し、型ごとに別コードを書き散らさない。**
共通の意思決定ループを1本持ち、差は `data/ai_params.tsv` のパラメータで出す。
型ごとに `.s` が増え始めたら設計を疑うこと。

実装開始は **P1（万能型1体）**、本格実装は **P2**。
