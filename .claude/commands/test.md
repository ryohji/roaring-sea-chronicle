---
description: 回帰テストを実行する（make test）。落ちたら原因を特定して報告する。
allowed-tools: Bash(make:*), Bash(python3:*), Read, Grep, Glob
---

`make test` を実行せよ。

- 落ちたテストがあれば、**テストを緩めずに**原因を特定して報告する（CLAUDE.md 第7条）。
- Mesen2 が無い環境では Python 6502 ハーネスで代替検証される。その旨も報告に含める。
